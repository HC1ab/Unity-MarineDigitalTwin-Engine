# SafeSail — 해양 디지털 트윈 기반 자율운항 AI 플랫폼
## 계획서용 프로젝트 설명

---

## 1. 프로젝트 한 줄 요약

> 실제 해양 기상 데이터를 반영한 3D 시뮬레이터에서 운항 데이터를 수집하고,
> AI가 위험을 예측하며 스스로 장애물을 피해 운항하는 자율운항 시스템

---

## 2. 우리가 만드는 것 (3가지)

### ① 해양 환경 시뮬레이터
실제 기상 API(기상청·국립해양조사원)에서 파고·풍속·조위 데이터를 받아
3D 바다 환경을 실시간으로 재현하는 Unity 시뮬레이터.
배의 물리 운동(추진·조타·부력)도 실제 선박 수학 모델(MMG)로 구현.

### ② LSTM 위험도 예측 AI
과거 30초간의 운항 데이터(속도·타각·레이다 센서·수심 등)를 분석해
앞으로 발생할 위험(충돌·좌초·과속)을 미리 예측하는 AI 모델.
예측한 위험도를 시뮬레이터 화면에 실시간 HUD로 표시.

### ③ PPO 자율운항 AI
레이다 센서 9개로 주변 장애물 거리를 감지하고,
수천 번의 시행착오 학습을 통해 스스로 장애물을 피하며
목표 지점까지 운항하는 자율운항 에이전트.

---

## 3. 전체 시스템 플로우

```
┌──────────────────────────────────────────────────────────┐
│  STEP 1. 실제 해양 환경 데이터 수집                       │
│                                                          │
│  기상청 부이 API ──→ 파고 · 풍속 · 풍향                  │
│  국립해양조사원 API ─→ 조위(바닷물 높이)                  │
│  수심 데이터 ────────→ 암초·얕은 바다 위치               │
│                  ↓                                       │
│          백엔드 서버(SafeSail)가 1시간마다 수집·저장      │
└──────────────────────────────────────────────────────────┘
                          ↓
┌──────────────────────────────────────────────────────────┐
│  STEP 2. Unity 3D 시뮬레이터에서 환경 재현                │
│                                                          │
│  파고 → 파도 높이·방향 반영                              │
│  풍속 → 바람 세기 반영                                   │
│  조위 → 해수면 높이 반영                                 │
│  수심 → 좌초 위험 구역 반영                              │
│                                                          │
│  + 레이다 센서 9개가 주변 40m~100m 장애물 거리 측정      │
└──────────────────────────────────────────────────────────┘
                          ↓
┌──────────────────────────────────────────────────────────┐
│  STEP 3. 운항 데이터 수집 (학습 데이터 생성)              │
│                                                          │
│  사람이 다양한 환경 조건(파고·풍속 등)을 설정한 뒤       │
│  시뮬레이터를 돌리면 매 5초마다 아래 데이터가 DB에 저장: │
│                                                          │
│  · 선박 상태: 속도, 방향, 타각, 흘수, 롤·피치           │
│  · 환경 상태: 파고, 풍속, 풍향, 조위                    │
│  · 센서 데이터: 레이다 9개 거리값, 선저 여유수심         │
│  · 이벤트 레이블: 충돌경고 / 좌초경고 / 과속 여부        │
└──────────────────────────────────────────────────────────┘
                          ↓
            ┌─────────────────────────────┐
            │  데이터 충분히 쌓인 후       │
            └──────────┬──────────────────┘
          ┌────────────┘ └──────────────────┐
          ↓                                 ↓
┌──────────────────────┐       ┌────────────────────────────┐
│  TRACK A             │       │  TRACK B                   │
│  LSTM 위험도 예측 AI  │       │  PPO 자율운항 AI            │
│                      │       │                            │
│  30초 운항 데이터     │       │  레이다 9개 + 속도·방향     │
│  → AI가 패턴 학습     │       │  → 수천 번 시행착오 학습   │
│  → 미래 위험도 예측   │       │  → 장애물 회피 운항 터득   │
│  → 화면에 HUD 표시    │       │  → 자율운항 데모           │
└──────────────────────┘       └────────────────────────────┘
          ↓                                 ↓
          └─────────────────────────────────┘
                          ↓
┌──────────────────────────────────────────────────────────┐
│  STEP 5. Unity 탑재 및 최종 데모                          │
│                                                          │
│  학습된 두 AI 모델을 Unity 시뮬레이터에 탑재             │
│  · 위험도 HUD: 실시간으로 위험 점수 0.0~1.0 표시         │
│  · 자율운항: 배가 스스로 장애물 피하며 목표 도달         │
│  · 세션 종료 후 운항 점수·이벤트 통계 결과 화면 표시     │
└──────────────────────────────────────────────────────────┘
```

---

## 4. 핵심 용어 설명

### MMG 모델 (Maneuvering Modeling Group)
실제 선박 운동을 수학으로 표현한 물리 모델.
배의 추진력(프로펠러), 조타력(키), 선체에 작용하는 물의 저항을 각각 계산해 합산한다.
일반 게임처럼 단순히 속도를 더하는 게 아니라, 실제 배처럼 관성·편류·선회 반경이 생긴다.

### HDRP (High Definition Render Pipeline)
Unity의 고품질 렌더링 시스템.
바다 파도, 빛 반사, 물속 굴절 등 사실적인 해양 환경을 표현하기 위해 사용.
HDRP의 `WaterSurface` 기능으로 파고·풍속·풍향에 따라 파도 모양이 실시간으로 변한다.

### 부력 시뮬레이션 (BuoyancySystem)
배 밑바닥 10개 지점에서 매 프레임 수면 높이를 측정해 각 지점별로 부력을 계산.
배가 파도 위에서 자연스럽게 흔들리고 기울어지는 효과를 구현.

### 레이다 센서 (RayPerceptionSensor)
배 주변 360°를 9개 방향으로 나눠 각 방향으로 보이지 않는 레이저를 쏘는 가상 센서.
장애물(암초·부두 등)에 닿으면 거리를 반환한다.
실제 선박의 레이다와 같은 개념. 값이 낮을수록 장애물이 가깝다는 뜻.

### LSTM (Long Short-Term Memory)
시간 순서가 있는 데이터를 학습하는 AI 모델의 일종.
"지금 이 순간"만 보는 게 아니라 "지난 30초의 흐름"을 함께 분석해 미래를 예측한다.
배는 관성이 있기 때문에, 현재 상태만으로는 위험 예측이 어렵다.
예를 들어 레이다 값이 점점 줄어드는 추세를 감지해 "5초 후 충돌 위험"을 미리 알린다.

### ONNX (Open Neural Network Exchange)
Python에서 학습한 AI 모델을 다른 환경(Unity 등)에서도 쓸 수 있게 변환하는 표준 파일 형식.
`.onnx` 파일 하나로 Python에서 만든 모델을 Unity에서 그대로 실행할 수 있다.

### Unity Sentis
Unity 공식 AI 추론 엔진.
ONNX 파일을 Unity 안에서 읽어 실시간으로 AI 추론을 실행한다.
외부 서버 없이 시뮬레이터 안에서 바로 AI가 동작하므로 지연(latency)이 없다.

### PPO (Proximal Policy Optimization)
강화학습(RL) 알고리즘 중 하나. 안정적이고 수렴이 빠른 편이라 게임 AI에 가장 많이 쓰인다.
에이전트(배)가 행동 → 보상/벌점 → 다음 행동 개선을 반복하며 최적 운항 전략을 찾는다.

### 강화학습 (Reinforcement Learning, RL)
AI가 정답 데이터 없이 **시행착오**로 스스로 학습하는 방식.

```
행동(Action)   → 결과(Reward/Penalty)  → 다음 행동 개선
배를 오른쪽으로  → 암초 충돌 → 벌점 -5  → "오른쪽은 위험"
배를 왼쪽으로   → 통과 성공 → 보상 +3  → "왼쪽이 낫다"
```

수천~수만 번 반복하면 배가 스스로 최적 경로를 찾는다.

### Unity ML-Agents
Unity에서 강화학습을 할 수 있게 해주는 공식 도구.
시뮬레이터 환경(Unity)과 학습 알고리즘(Python PPO)을 연결해주는 다리 역할.
Unity 안에서 배를 수천 번 자동 시뮬레이션하며 학습한다.

### 텔레메트리 (Telemetry)
운항 중 실시간으로 수집되는 모든 측정 데이터의 통칭.
선박 상태(속도·방향·타각) + 환경 상태(파고·풍속) + 센서 데이터(레이다) + 이벤트를 포함.
5초마다 백엔드 DB에 저장되어 AI 학습 데이터로 활용된다.

### EvaluationService (세션 평가)
운항 세션이 종료될 때 자동으로 운항 점수를 계산하는 백엔드 서비스.
충돌 횟수, 과속 횟수, 항로 이탈 거리 등을 종합해 totalScore 산출.

---

## 5. 기술 스택 요약

| 구분 | 기술 | 용도 |
|------|------|------|
| 시뮬레이터 | Unity 6 LTS + HDRP | 3D 해양 환경 렌더링 |
| 선박 물리 | MMG 모델 (C#) | 실제 선박 운동 구현 |
| 환경 연동 | UnityWebRequest | 백엔드 API 폴링 |
| 센서 | RayPerceptionSensor × 9 | 장애물 거리 측정 |
| AI 추론 | Unity Sentis (ONNX) | 게임 내 온디바이스 AI |
| 위험도 AI | LSTM (PyTorch) | 시계열 위험 예측 |
| 자율운항 AI | ML-Agents PPO | 강화학습 자율운항 |
| 백엔드 | Spring Boot 3 / Java 21 | API 서버 |
| DB | PostgreSQL + PostGIS | 운항 로그 저장 |
| 캐시 | Redis | 기상 데이터 캐시 |
| 공공 API | 기상청 · 국립해양조사원 | 실제 해양 기상 수집 |

---

## 6. 개발 단계별 목표

| 단계 | 기간 | 목표 |
|------|------|------|
| 환경 구축 | 완료 | 실제 기상 연동 해양 시뮬레이터 |
| 데이터 수집 | 1~2주 | 레이다 센서 + 이벤트 감지 + DB 로그 |
| LSTM 위험도 AI | 3주 | 위험 예측 모델 학습 + HUD 탑재 |
| PPO 자율운항 AI | 4~5주 | 장애물 회피 자율운항 학습 + 데모 |
| 통합·마무리 | 6주 | 결과 화면 + 최종 데모 완성 |
